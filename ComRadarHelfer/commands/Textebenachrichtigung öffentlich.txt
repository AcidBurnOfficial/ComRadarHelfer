import discord
from discord import app_commands
from discord.ext import commands
from config import TEST_GUILD_ID
from utils.permissions import restricted_command


class Wurmi(commands.Cog):
    def __init__(self, bot):
        self.bot = bot

    @app_commands.command(name="wurmi", description="Antwortet mit Schick Fußbilder!")
    @app_commands.guilds(discord.Object(id=TEST_GUILD_ID))
    @restricted_command()
    async def wurmi(self, interaction: discord.Interaction):
        # Nachricht wird öffentlich im Channel gesendet:
        await interaction.response.send_message("👣 Schick Fußbilder!")


async def setup(bot):
    await bot.add_cog(Wurmi(bot))
